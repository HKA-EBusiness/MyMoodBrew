<manifest xmlns:android="http://schemas.android.com/apk/res/android"  DevSkim: ignore DS137138
    xmlns:tools="https://schemas.android.com/tools"
    package="com.example.mymoodbrew_v2">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:name="com.example.mymoodbrew_v2.MyMoodBrew"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyMoodBrew_V2"
        tools:targetApi="30">
        <activity
            android:name="com.example.mymoodbrew_v2.MainActivity"
            android:exported="true"
            tools:ignore="MissingClass">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>